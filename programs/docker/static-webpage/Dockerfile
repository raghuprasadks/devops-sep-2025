# Use the official Nginx image as the base
FROM nginx:alpine

# Copy your index.html file into the container's web directory
COPY index.html /usr/share/nginx/html/index.html

# The Nginx container will automatically serve this file on port 80